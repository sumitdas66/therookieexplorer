---
import { getPhotos } from '../lib/photos.js';
const photos = getPhotos('moments');
const UNIT_REPEATS = 6;
const duration = photos.length * UNIT_REPEATS * 4;
const extraPasses = Array.from({ length: UNIT_REPEATS * 2 - 1 });
---
{photos.length > 0 && (
  <section class="moments-strip" data-animate="fade-up">
    <div class="moments-track" style={`animation-duration: ${duration}s;`}>
      {photos.map((src) => <img class="moment-photo" src={src} alt="" loading="eager" />)}
      {extraPasses.flatMap(() =>
        photos.map((src) => (
          <img class="moment-photo moment-photo-dup" src={src} alt="" loading="eager" aria-hidden="true" />
        ))
      )}
    </div>
  </section>
)}

<style>
  .moments-strip {
    overflow: hidden;
    padding: var(--space-base) var(--space-xl);
  }
  .moments-track {
    display: flex;
    gap: var(--space-base);
    width: max-content;
    animation-name: moments-slide;
    animation-timing-function: linear;
    animation-iteration-count: infinite;
    will-change: transform;
  }
  .moments-strip:hover .moments-track {
    animation-play-state: paused;
  }
  .moment-photo {
    height: 220px;
    width: auto;
    border-radius: var(--radius-md);
    flex-shrink: 0;
  }
  @keyframes moments-slide {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }
  @media (prefers-reduced-motion: reduce) {
    .moments-strip {
      overflow-x: auto;
    }
    .moments-track {
      animation: none;
    }
    .moment-photo-dup {
      display: none;
    }
  }
</style>
